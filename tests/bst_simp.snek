(fun (createBST rootVal)
    (vec rootVal false false)
)
(fun (addNode rootNode nodeVal)
    (if (isbool rootNode)
        (createBST nodeVal)
        (let (
                (root_val  (vec-get rootNode 0))
                (left_val  (vec-get rootNode 1)) 
                (right_val  (vec-get rootNode 2))
            )
                (if (< (vec-get rootNode 0) nodeVal) 
                    (let ((subtree (addNode right_val nodeVal) ))
                        (vec root_val left_val subtree)
                    )
                    (if (> (vec-get rootNode 0) nodeVal) 
                        (let ((subtree (addNode left_val nodeVal) ))
                            (vec root_val subtree right_val)
                        )         
                        rootNode   
                    )
                )
        )
    )
)
(fun (searchBST rootNode searchVal)
    (if (isnum searchVal)
        (if (isbool rootNode)
            false
            (let (
                    (root_val  (vec-get rootNode 0))
                    (left_val  (vec-get rootNode 1)) 
                    (right_val  (vec-get rootNode 2))
                )
                    (if (< (vec-get rootNode 0) searchVal) 
                        (searchBST right_val searchVal)
                        (if (> (vec-get rootNode 0) searchVal) 
                            (searchBST left_val searchVal) 
                            true   
                        )
                    )
            )
        )
        false
    )
)
(block (addNode (addNode (vec 2 false false) 3) 1) (snek-printheap))